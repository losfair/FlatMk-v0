#pragma once

#include <spec.h>

static struct TaskEndpoint CAP_FRAMEBUFFER = { 0x10 };

static struct TaskEndpoint CAP_POLL_INPUT = { 0x11 };

static struct TaskEndpoint CAP_BUFFER_INITRET = { 0x12 };
